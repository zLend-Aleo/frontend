const routes = {
  admin: '/admin',
  deploy: '/deploy',
  initialize: '/initialize',
  whitelist: '/whitelist',
  add: '/add',
  settings: '/settings',
  authorize: '/authorize',
  test: '/test'
};

export default routes;
